# Cleanup Proposal — `refactor` branch

Scope: cleanup of the review feature work just shipped on the `refactor` branch (VCV-183 DHIS2 export submission page + VCV-213 metadata review conflict-resolution fix). Goal: simplify without changing any functionality or UI.

---

## Status overview

| Step | PRD items | Status |
|---|---|---|
| **Step 1** | A1 | ✅ Applied |
| **Step 2** | A2 + G + H + F2 | ✅ Applied |
| **Step 3** | D | ✅ Applied |
| **Step 4** | E + partial B + Checkbox swaps + Collapsible restructure | ✅ Applied |
| **Step 5** | F1 | ✅ Applied |
| **Side change** | shared/ → components/ folder rename | ✅ Applied |
| **Side change** | DHIS2 `siteIds` query param as real array | ✅ Applied |
| **Step 6 (next)** | A3 + A4 + B remainder + C | ⏳ Pending — revised scope |
| **A5 (wrapper delete)** | review-site-hierarchy.tsx | ❌ **Cancelled** — wrapper is justified, see below |
| **A6 (wrapper delete)** | export-site-hierarchy.tsx | ❌ **Cancelled** — wrapper is justified, see below |
| **Item I** | Naming polish | Absorbed into prior steps |
| **Item J** | `ReviewSiteSessionSummary` discussion | Open (kept as-is for now) |

---

## A. Delete files

### A1. Delete `src/features/review/export/utils/derive-site-statuses.ts` — ✅ Applied (Step 1)
Single caller in `review-export-list.tsx`. The export loop already runs **per site**, so the function's "build a Map keyed by site" structure was overkill. Inlined as ~10 lines (`some(failed) → 'failed'`, else `some(success) → 'success'`, else `'skipped'`).

### A2. Delete `src/features/review/export/utils/build-site-irs-data.ts` — ✅ Applied (Step 2)
Both exports violated the "no intermediate types" rule:
- `ExportBatchSite` was a flattened view of `(monthKey, siteId, district, irsData)` used by `runExport` only.
- `SiteIrsFormData` was a form-state composite — replaced with destructured primitives.

`buildExportItems` and `toSiteIrsData` deleted. The form→`SiteIrsData` conversion now lives inside `ExportConfirmDialog.handleStartExport` so the API domain type (`SiteIrsData`) crosses component boundaries, not a form intermediate.

### A3. Delete `src/features/review/sites-list/hooks/use-review-sites-list-month-key.ts` — ⏳ Pending (Step 6)
The React Context passes `monthKey` from `ReviewSitesList` down through `ReviewSiteHierarchy` to `ReviewSiteLeafRows`. With A5 cancelled, the replacement is **prop drilling**: add a `monthKey: string` prop to `ReviewSiteHierarchy`, which forwards it to `ReviewSiteLeafRows` via the existing render-prop closure. One prop, two layers — well within the "context only for cross-cutting state" threshold.

### A4. Delete `src/features/review/components/site-leaf-rows.tsx` — ⏳ Pending (Step 6)
*(Path updated from `shared/` to `components/` after the folder rename.)*
26-line file abstracting `filter` + `map` + `<div className="space-y-1">`. Below the 2-files-3-callsites bar, and the saved code is trivial. Replace both call sites with the same inline 3 lines.

### A5. ~~Delete `review-site-hierarchy.tsx`~~ — ❌ Cancelled
The original PRD framed this wrapper as "pure glue." That framing was wrong. The file encapsulates ~88 lines of **feature-specific logic** (visit-coverage color thresholds, percentage math, needsReview totals, badge composition) configuring the shared `SiteHierarchy` for review-mode display. It has a single, cohesive responsibility.

Inlining would grow `review-sites-list.tsx` from ~197 → ~280 lines and mix month-iteration + list-state + visit-coverage logic + leaf-row rendering closures into one file — worse on every dimension the user values (scalability, readability, single responsibility, cognitive complexity).

The "no helper layers waiting for a second consumer" rule applies to *speculative scaffolding*, not to concrete components doing real cohesive work today. **Kept.**

### A6. ~~Delete `export-site-hierarchy.tsx`~~ — ❌ Cancelled
Same reasoning as A5. The wrapper encapsulates feature-specific render-prop logic for export-mode (certified-count filtering, checkbox state, badge composition). **Kept.**

Note: dropping `ExportMonthContext` (PRD C) was originally tied to inlining this wrapper. Revised approach: replace the context with prop drilling through the kept wrapper. See Step 6 below.

---

## B. Reduce `export-selection-helpers.ts` — ✅ Mostly applied (Step 4); `hasCertifiedSessions` pending

`src/features/review/export/utils/export-selection-helpers.ts` was reduced from 7 functions to 2.

| Function | Status |
|---|---|
| `isMonthFullySelected` | ✅ Kept (used in 2 files). Signature changed to accept `Iterable<number>` so consumers can pass `Map.keys()` without allocating a Set. |
| `totalSelectedCount` | ✅ Inlined into `review-export-list.tsx` |
| `isAllSelected` | ✅ Inlined into `review-export-list.tsx` |
| `toggleSites` | ✅ Inlined into `handleToggleSites` in `review-export-list.tsx` |
| `selectAll` | ✅ Inlined into `handleSelectAll` in `review-export-list.tsx` |
| `groupExportResultsByMonth` | ✅ Deleted entirely (see D — results are now natively nested) |
| `hasCertifiedSessions` | ⏳ Pending — to be inlined when A6 inlines its consumers |

---

## C. Remove `ExportMonthContext` from `export-month-row.tsx` — ⏳ Pending (Step 6)

The context passes 5 pieces of state down to `ExportSiteHierarchy` and `ExportSiteLeafRows`. With A6 cancelled, the replacement is **prop drilling**: `ExportMonthRow` passes the 5 fields as props to `ExportSiteHierarchy`, which forwards them to `ExportSiteLeafRows` via the existing render-prop closure. State is not cross-cutting; it's hierarchical and shallow (2 layers). Context adds no value here.

---

## D. Nested Map for results — ✅ Applied (Step 3)

`exportResults` state changed from `Map<string, Dhis2ExportSiteStatus>` keyed by `${monthKey}:${siteId}` to `Map<string, Map<number, Dhis2ExportSiteStatus>>` (nested by monthKey then siteId).

This deleted the encode/decode dance entirely, removed the `exportStatusByMonth` `useMemo`, and removed the need for `groupExportResultsByMonth`. The downstream `ExportMonthRow` `exportStatusBySiteId` prop is already shaped per-month.

---

## E. Drop `certifiedSiteIdsByMonth` — ✅ Applied (Step 4)

`certifiedSiteIdsByMonth` was a derived `Map<string, Set<number>>` redundant with `certifiedCountsByMonth: Map<string, Map<number, number>>` (keys are identical). The second `useMemo` is gone; consumers read `certifiedCountsByMonth.get(monthKey)?.keys()` / `.size` / `.has(siteId)` directly.

---

## F. Inline module-level helpers in component files

### F1. `metadata-review-table.tsx` — ✅ Applied (Step 5)
`validateNumberInput` and `validateNonNegativeInput` were module-level helpers inside a component file (rule violation). Moved into the component scope as nested closures with clearer names:
- `validateNumberInput` → `validateWholePositiveInteger` (min ≥ 1)
- `validateNonNegativeInput` → `validateWholeNonNegativeInteger` (min ≥ 0)

### F2. `INSECTICIDE_OPTIONS` `export` dropped — ✅ Applied (Step 2)
The constant in `site-irs-row.tsx` was `export`ed but never imported elsewhere. Dropped the keyword.

---

## G. Strip the `SiteIrsFormData` intermediate type — ✅ Applied (Step 2)

The form→domain conversion now happens at the dialog's boundary instead of the `runExport` loop.

- `site-irs-row.tsx` — `entry: SiteIrsFormData` composite props replaced with destructured primitive props (`siteId`, `wasIrsSprayed`, `insecticideSprayed`, `dateLastSprayed`, `onChange`).
- `export-confirm-dialog.tsx` — internal form state uses an inline shape; `onConfirm` callback type is `Map<number, SiteIrsData>` (the API domain type).
- `review-export-list.tsx` — `handleConfirmExport` receives `Map<number, SiteIrsData>` directly; `runExport` iterates `selectedSites` Map directly, no batch list needed.

---

## H. Simplify `runExport` — ✅ Applied (Step 2, refined in Step 3)

After Step 2, `runExport` iterates `selectedSites` directly with no intermediate `exportSites: ExportBatchSite[]` array. After Step 3, result accumulation uses a nested Map.

---

## I. Naming polish — ✅ Absorbed into prior steps

Applied during refactors:

- `counts` → `countsForMonth` (review-export-list.tsx)
- `current` → `existingSiteSummary` (review-sites-list.tsx)
- `result` → `postDhis2ExportResult` (review-export-list.tsx)
- `entry` / `data` → `siteIrsForm` / `initialIrsFormBySiteId` (export-confirm-dialog.tsx)
- `mutateAsync` → `submitDhis2Export` (review-export-list.tsx destructure)
- `ExportSiteStatus` → `Dhis2ExportSiteStatus` (the type itself — reflects DHIS2 origin)

---

## J. Discussion item — `ReviewSiteSessionSummary`

`src/features/review/sites-list/utils/review-site-session-summary.ts` defines a 5-field derived view of session state. Strictly read, this is an "intermediate type outside the main domain models." It flows through 3 components as `Map<number, ReviewSiteSessionSummary>` and inlining the shape 3× would be duplicative.

**Decision: kept as-is for now.** Practical trade-off; revisit if the shape spreads further.

---

## Side change — `shared/` → `components/` folder rename — ✅ Applied

`src/features/review/shared/` was renamed to `src/features/review/components/` to align with the convention in `src/features/operations/`, where the top-level `components/` folder holds cross-cutting components shared across sub-features. All four imports (in `review-site-hierarchy.tsx`, `review-site-leaf-rows.tsx`, `export-site-hierarchy.tsx`, `export-site-leaf-rows.tsx`) were updated.

---

## Side change — DHIS2 `siteIds` query param as array — ✅ Applied

The backend `siteIds` query param accepts an array. The frontend schema was using `z.string().optional()` and the caller was passing `String(siteId)`. Switched to:

- Schema (`post-dhis2-export-schema.ts`): `siteIds: arrayQueryParamSchema.optional()` (matching the pattern in `get-sessions-report-schema.ts`).
- Call site (`runExport`): `siteIds: [siteId]` instead of `siteIds: String(siteId)`.

Wire format unchanged (`?siteIds=42` either way; `constructQueryString` and `arrayQueryParamSchema.preprocess` handle the comma-joined round-trip).

---

## Side change — shadcn `Checkbox` swap across export feature — ✅ Applied (Step 4)

Raw `<input type="checkbox">` was replaced with shadcn `<Checkbox>` (from `@/components/ui/checkbox`) in:

- `site-irs-row.tsx` (Step 2)
- `export-confirm-dialog.tsx` — already uses Checkbox indirectly via SiteIrsRow
- `export-month-row.tsx` (Step 4)
- `export-site-hierarchy.tsx` (Step 4)
- `export-site-leaf-rows.tsx` (Step 4)

For the two locations where the raw input had been nested inside a `<CollapsibleTrigger>` (a `<button>`), the layout was restructured to put the Checkbox as a **sibling** of the trigger rather than a child. Without this, button-in-button HTML would be invalid. The restructure was applied in:

- `src/features/review/components/collapsible-location-group.tsx` — `prefixContent` now renders inside the outer flex wrapper, alongside `CollapsibleTrigger` (which takes `flex-1`). Hover styling moved to the outer wrapper. Same visual layout, same gap/padding/cursor semantics. Review-side (no `prefixContent` passed) is rendered identically.
- `src/features/review/export/components/export-month-row.tsx` — same sibling restructure inline.

`event.stopPropagation()` on the checkboxes is no longer needed (siblings of the trigger can't bubble into it).

---

## Side change — Other renames

- `Dhis2SyncResult['status']` exposed as `Dhis2ExportSiteStatus` (was `ExportSiteStatus`). Reflects DHIS2 origin and feature usage.

---

## Step 6 (pending) — context + thin-helper cleanup (revised scope)

Bundles the remaining items A3 + A4 + B remainder + C. A5 and A6 are cancelled (see above). The wrappers stay; the cleanup now targets the unjustified abstractions inside and around them.

Changes:

- **A3** — Delete `src/features/review/sites-list/hooks/use-review-sites-list-month-key.ts`. Replace the `ReviewSiteListMonthKeyContext` with a `monthKey: string` prop on `ReviewSiteHierarchy`, forwarded to `ReviewSiteLeafRows` via the existing render-prop closure.
- **A4** — Delete `src/features/review/components/site-leaf-rows.tsx`. 26-line file wrapping `filter+map+<div className="space-y-1">`. Inline the 3-line pattern at the 2 call sites (`review-site-leaf-rows.tsx`, `export-site-leaf-rows.tsx`).
- **B remainder** — Inline `hasCertifiedSessions` at its 2 call sites in `export-site-hierarchy.tsx` and `export-site-leaf-rows.tsx`. The 1-line predicate `(certifiedCountsBySiteId.get(site.siteId) ?? 0) > 0` is clearer inline.
- **C** — Delete `ExportMonthContext` (and `useExportMonthContext`) from `export-month-row.tsx`. Pass the 5 state fields as props through `ExportSiteHierarchy` to the render-prop callback for `ExportSiteLeafRows`.

After Step 6, `export-selection-helpers.ts` will be down to a single function (`isMonthFullySelected`).

**Risk profile:** very low. No rendering structure changes, no wrapper inlinings — just context-to-props swaps and small helper inlines. Visual regression risk is near zero.

---

## Implementation rules (reference)

These govern how the cleanup is carried out, not what it produces. Every item above respects:

- **Descriptive variable names.** Every identifier conveys what it holds. Not `value`/`v`/`q`/`first` — use `prerequisiteExpression`/`predicateValue`/`referencedQuestion`/`firstReferencableQuestion`. If a reader has to consult the type to understand the variable, the name is wrong. Applies to props, locals, callback parameters, destructured pieces.
- **Minimal implementation.** Build only what the current step requires. No speculative scaffolding, no "we'll probably want X later," no helper layers waiting for a second consumer, no flag-guarded experimental code paths. Three similar lines beats a premature abstraction.
- **No intermediate types outside the main domain models.** Contract schemas under `src/api/<resource>/validation/` are the only named data shapes. Components and utilities operate directly on those types — no editor models, no flattened intermediate views. React-state pieces stay as primitive destructured fields without naming the composite. Component props interfaces are fine; they describe a component's surface, not a reusable data shape.
- **Utility functions only when absolutely necessary.** A util in `src/features/<feature>/utils/` earns its place only when the alternative is real duplication. Bar: ~3 call sites across ≥2 files of non-trivial logic. A 3-line helper used once is inlined. A helper used twice within a single file is inlined or expressed as a closure inside the consuming function. No speculative utilities, no `parse`/`serialize` pairs, no factory functions, no module-level helpers defined inside component files.
- **No overengineering.** If the natural expression is a 5-line inline computation, that's the expression. If the natural expression is a 15-line cross-file switch, that earns a util. Nothing in between gets abstracted. Premature flexibility (options parameters, hooks-as-props, generic wrappers) is rejected by default and only added when the second real consumer materializes.
- **Use shadcn/ui components wherever possible.** Reach for `src/components/ui/*` (Checkbox, Button, Input, Select, Dialog, etc.) instead of raw HTML elements. Restructure layout if the established convention (e.g., nesting a button-based primitive inside another button-based primitive) would produce invalid HTML.
